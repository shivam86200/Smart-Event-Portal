apiVersion: v1
kind: Service
metadata:
  name: smart-event-portal-service
  labels:
    app: smart-event-portal
spec:
  type: NodePort
  selector:
    app: smart-event-portal
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      nodePort: 30081
